{
    "categories": [
        {
            "title": "## 🚀 Features",
            "labels": ["feature", "feat", "add", "添加"]
        },
        {
            "title": "## 🐛 Fixes",
            "labels": ["fix","修复"]
        },
        {
            "title": "## 🧪 Docs",
            "labels": ["README"]
        },
        {
            "title": "## 🧪 Update",
            "labels": ["update"]
        }
    ],
    "label_extractor": [
        {
            "pattern": ".*(?i)(feature|feat|add|添加).*",
            "target": "$1",
            "on_property": "title"
        },
        "label_extractor": [
        {
            "pattern": ".*(?i)(fix|修复).*",
            "target": "$1",
            "on_property": "title"
        },
        "label_extractor": [
        {
            "pattern": ".*(?i)README.*",
            "target": "$1",
            "on_property": "title"
        },
            "label_extractor": [
        {
            "pattern": ".*(?i)update.*",
            "target": "$1",
            "on_property": "title"
        }
    ],
    "sort": "ASC",
    "template": "${{CHANGELOG}}",
    "pr_template": "- ${{TITLE}}\n   - PR: #${{NUMBER}} by#{{OWNER}}",
    "empty_template": "- no changes",
    "max_pull_requests": 1000,
    "max_back_track_time_days": 1000
}
